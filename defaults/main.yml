---
coreos_bootstrap_dir:          '/opt/bin'
coreos_bootstrap_bin_dir:      '/usr/bin'

coreos_bootstrap_pypy_version:       '5.6.0'
coreos_bootstrap_pypy_download_dest: '/tmp'

coreos_bootstrap_pip_download_dest: '/tmp'
